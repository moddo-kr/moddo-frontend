// import { http, HttpResponse, passthrough } from 'msw';
// import getIsMocked from '@/mocks/utils/getIsMocked';

export const dummyGroupMembers = new Map([
  [1, { id: 1, name: '김반숙', role: 'MANAGER' }],
  [2, { id: 2, name: '박완숙', role: 'PARTICIPANT' }],
  [3, { id: 3, name: '정에그', role: 'PARTICIPANT' }],
]);

// const groupMemberHandlers = [];

// export default groupMemberHandlers;
